# Security Policy

## 报告漏洞

请优先使用 GitHub 仓库的 **Private vulnerability reporting** 提交安全问题。若仓库尚未启用该功能，请联系维护者后再发送复现材料。不要在公开 Issue 中发布 API Key、私人录音、完整命令、数据库或本机路径。

报告应包含受影响版本、最小复现步骤、预期与实际行为。请使用无隐私内容的测试文件。

## 安全边界

- 后端默认仅监听 `127.0.0.1`；本项目不是面向公网部署的多租户服务。
- `.env`、SQLite、参考音频、生成音频和模型文件不得进入发布包或 Git。
- 云端提供商会接收完成请求所需的数据，使用前应阅读其隐私与计费条款。
- **Local Command** 会启动用户配置的本机程序。它默认禁用，不是沙箱，也不会把不可信脚本变安全。只运行你理解且信任的命令。
- 桌面 sidecar 的错误输出可能来自第三方工具；分享日志前请先脱敏。

当前维护范围为最新公开版本。修复通常先进入主分支，再随补丁版本发布。
