# TTS WebUI Workbench 0.1.0

这是首个公开预览版，重点是提供一个可验证、不会携带维护者私人配置的 Windows TTS 工作台。

## 使用入口

- 普通用户：下载 `tts-webui-workbench-v0.1.0-windows-x64.zip`，解压后运行 `app.exe`。
- 开发者：按 `README.md` 和 `docs/SETUP_GUIDE.md` 从源码启动。

## 首次启动

首次安装只启用 Dummy 测试音。Higgs、本地 HTTP、本地命令和 Piper 会显示为示例但默认禁用，需用户完成配置后再启用。

## 数据边界

发布包不含 API Key、数据库、生成音频、参考音频、模型权重或维护者声音。使用云端提供商时，请自行确认其费用、隐私和许可条款。

## 已知限制

- 主要验证平台是 Windows 10/11 x64。
- 第三方本地模型和可执行文件不随应用分发。
- 本地命令不是沙箱，只能运行可信配置。
- 真实云端合成未纳入默认离线测试，以免产生费用。
